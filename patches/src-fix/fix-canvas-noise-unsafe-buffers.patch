description: opt static_bitmap_image.cc out of -Wunsafe-buffer-usage; the core
 canvas-noise patch uses raw-pointer pixel access.
author: bearinmindcat <bearinmindcat@gmail.com>

--- a/third_party/blink/renderer/platform/graphics/static_bitmap_image.cc
+++ b/third_party/blink/renderer/platform/graphics/static_bitmap_image.cc
@@ -2,6 +2,11 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
+#ifdef UNSAFE_BUFFERS_BUILD
+// TODO(crbug.com/351564777): canvas-noise uses raw-pointer pixel access.
+#pragma allow_unsafe_buffers
+#endif
+
 #include "third_party/blink/renderer/platform/graphics/static_bitmap_image.h"
 
 #include "base/rand_util.h"
